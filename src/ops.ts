import { Operation } from "slate";
import { SyncDocument } from "./types/sync";

export const applyOperation = (doc: SyncDocument, op: Operation): SyncDocument => {
  // switch (op.type) {
  //   case 'add_mark' {
  //     op.
  //   }
  // }
  return doc;
}